# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# TLS certificates and keys (security sensitive)
*.crt
*.key
*.pem
*.pfx
server.cert
server.key
server.crt

# Configuration files (but keep examples)
config.json
test_config.json
dev_config.json
prod_config.json
local_config.json
# Keep example files for documentation
!config.example.json
!*.example.json

# Log files
*.log
server_log.txt

# Application specific files
urls.json
coverage.out

# IDE files
.vscode/
.idea/
*.swp
*.swo

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Build artifacts
/build/
/dist/
staticlint.exe
staticlint
shortener.exe
shortener
api.exe
api
test_shortener.exe
